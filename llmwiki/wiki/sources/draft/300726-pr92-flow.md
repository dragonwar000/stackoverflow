---
type: draft
title: 300726-pr92-flow
status: proposed
tags: [docs-site-macos, pr-review, output-report]
timestamp: 2026-07-30
---

# 300726-pr92-flow

**Status:** proposed

## What

Trang HTML macOS-glass giải thích PR #92 (`github.com/Rheinmir/setup/pull/92`, `graph-engineering` → `orca`): vấn đề nó giải quyết (3 gốc thiếu quy từ đối chiếu 67 mục spec/PDF), luồng chạy của 7 tính năng T1–T7, và — trọng tâm câu hỏi của user — **cách gọi từng tính năng + điều kiện thật sự kích hoạt** (đa số là opt-in/gọi tay, chỉ 2/7 tự động qua hook có sẵn).

## Output

- `llmwiki/html/300726-pr92-flow.html` — 1 file self-contained, sidebar nav, mind map collapsible, 9 section (tổng quan · bản đồ kích hoạt · T1–T7 · rủi ro trước merge), mỗi section có sơ đồ SVG kéo-thả minh hoạ luồng thật (đọc trực tiếp từ code, không suy đoán từ tên hàm)
- Bảng "khi nào kích hoạt" xác nhận bằng grep thật: chỉ `wiki-graph.py` (T2, qua hook `llmwiki/.claude/hooks/stop.py`) và skill `/query` (T3) chạy tự động; T1/T4/T5/T6/T7 không có hook nào gọi, phải chạy tay qua CLI
- Kèm tóm tắt 4 mục blocking + 5 mục nên sửa từ phiên qc-code review đã chạy trước đó trong cùng hội thoại

## Files
| File | Action |
|------|--------|
| `llmwiki/html/300726-pr92-flow.html` | created |

## Notes
- Invoked via: `/docs-site-macos`, nối tiếp phiên qc-code review PR #92 trong cùng hội thoại
- Xác nhận trigger bằng grep thật trên `llmwiki/.claude/hooks/`, không đoán từ docstring: `stop.py` gọi `wiki-graph.py` và `provenance-log.py record-changed` (wiring cũ, không phải feature mới của PR); không hook nào gọi `loop-runner.py`, `hub.py`, `grounding-check.py`, hay `provenance-log.py post-hypothesis`
- Xem tại `http://localhost:8765/llmwiki/html/300726-pr92-flow.html`

## Origin
- **Draft:** `wiki/sources/draft/300726-pr92-flow.md`
- **Commit:** _(filled by verify-before-commit)_
- **Date promoted:** _(filled by verify-before-commit)_
