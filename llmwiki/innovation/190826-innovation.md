---
title: "Overstack vs thế giới — 19/08/2026: spec-driven mainstream hoá, provenance 6-chiều, self-evolving skills có thiết kế cụ thể"
status: living
tags: [innovation, frontier, comparison, gap-analysis, last30days]
timestamp: 2026-08-19
id: innovation-190826
---

# Overstack vs thế giới — rà soát 19/08/2026

Tiếp nối [[innovation-180826]] (18/08: sandbox cách ly, agent teams, receipt xác minh). Kỳ này trước tiên sửa lại bản cài `last30days` — thư mục `~/.agents/skills/last30days` chỉ còn mỗi `SKILL.md` (thiếu cả `scripts/`), do đó không chạy được; đã trỏ lại symlink về repo gốc đầy đủ `~/orca/last30day/last30days-skill/skills/last30days` rồi mới chạy nghiên cứu. Chạy 2 `WebSearch` tổng quan (framework landscape 2026, agent memory/provenance research 2026) + 1 lượt engine `last30days.py` (Reddit/HN/GitHub/YouTube thật, không có API key social nên thiếu X/TikTok) với 3 subquery: multi-agent orchestration framework, spec-driven development workflow, agent memory/provenance/evidence-chain. Đối chiếu với `fdk/CAPABILITIES.md`, `skills/graph-mode/SKILL.md` (luật R19 evidence-terminal), draft `llmwiki/wiki/sources/draft/140726-spec-kit-traceability.md`, và `llmwiki/wiki/sources/ISSUES.md` (GH#10, GH#12 xác nhận vẫn `open` qua grep trực tiếp) để tránh claim trùng.

## Bảng đối chiếu

| Trục | 🔵 Overstack đang có | 🟢 Ý tưởng mới thế giới (30 ngày qua) | Verdict |
|---|---|---|---|
| **Hệ sinh thái framework đa-agent** | `Workflow` tool: `pipeline()`/`parallel()` không đồng bộ giữa các stage, gom theo `phase()`, budget-aware scaling, cache-resume theo `runId`; GH#12 (DAG + file-bus, hàng-trăm-subagent có verify) vẫn mở | LangChain tổng hợp landscape 2026: LangGraph (orchestration có trạng thái, checkpointing, nhánh/retry phức tạp), AutoGen đã hợp nhất vào Semantic Kernel cho production, OpenAI Agents SDK tối giản (agent gọi agent như tool), DSPy (module LLM khai báo, prompt được compile+tối ưu tự động), MetaGPT (vai trò cố định kiến-trúc-sư/kỹ-sư/tester cho SW eng); mô hình phổ biến 2026: 3 tier (interactive / parallel sprint / drain-backlog-qua-đêm) | **Ngang phần lõi, thua lẻ 2 điểm.** `pipeline`/`parallel` đã tương đương LangGraph ở phần fan-out có cấu trúc (xác nhận lại từ 18/08). Chưa có: (1) compile-tối-ưu-prompt kiểu DSPy — agent() hiện viết prompt tĩnh tay; (2) vai trò cố định kiểu MetaGPT cho pipeline SW-eng dài hơi. GH#12 (hàng trăm subagent) vẫn đúng hướng, chưa build |
| **Chuỗi chứng cứ / provenance có cấu trúc** | R19 `evidence-terminal` (graph-mode, opt-in CHAT): mọi lập luận phải chấm dứt ở 1 trong 6 loại điểm cuối — `observed`/`tool-record`/`graph-edge`/`web`/`parametric`/`absence`; validator máy `evidence_terminal.py` cắn độc lập với graph-mode | Survey "From Agent Traces to Trust" (arXiv 2606.04990, 2026) đề xuất khung provenance **6 chiều**: trace sources, evidence & execution units, provenance relations, granularity/timing, representation forms, **trust functions** (tính điểm tin cậy tổng hợp từ chuỗi provenance) | **Song song thật, thiếu 1 chiều.** 6 loại điểm cuối của R19 ánh xạ gần khớp "evidence & execution units" + "trace sources" của academic framework — không phải trùng ngẫu nhiên, cùng bài toán. Thiếu hẳn "trust functions": R19 phân loại NGUỒN chứng cứ, không tính điểm tin cậy hay phát hiện xung đột khi 2 điểm cuối mâu thuẫn nhau |
| **Skill tự tiến hoá có bằng chứng đi kèm** | GH#10 "Self-evolving skills: vòng CoEvoSkills nội bộ (sinh→eval→merge)" — mở, chưa build; `failure-flywheel` hiện tại chỉ đếm lỗi lặp rồi scaffold rule/skill stub trơn, không gắn evidence-link hay ước lượng độ tin cậy | "From Memory to Skills: Evidence-Grounded Co-Evolution Governance" (arXiv 2607.16621, 2026) — MSCE: kết tinh policy từ trace kinh nghiệm thành skill callable **mang theo** evidence link, applicability boundary, decision guidance, verification rule, reliability estimate | **Thua — củng cố ưu tiên GH#10, không phải gap mới.** Bài toán đã có issue mở đúng hướng; MSCE cho một THIẾT KẾ CỤ THỂ (5 trường mỗi skill sinh ra) mà GH#10 hiện chưa có, và `failure-flywheel` là bản rất mỏng so với 5 trường đó |
| **Spec-driven development mainstream hoá** | `/br compile` đã sinh `clause_id` cho từng điều khoản BR (đã ship); draft `140726-spec-kit-traceability` đề xuất mang `FR-xxx`/`SC-xxx` + cổng truy vết sang nhánh nhẹ `/propose`+`/plan` (status **proposed**, SPEC đã viết xong, **chưa build**) | Cộng đồng đang thực hành hoá spec-driven dev qua khoá DeepLearning.AI ("Spec-Driven Development"); PR thật quan sát được trên GitHub (2026-08-15): dev retrofit `specs/mission.md` vào repo hiện có theo đúng phương pháp đó — không còn là ý tưởng lý thuyết, đã thành thao tác hàng ngày | **Có nền, thiếu ở nhánh nhẹ — trend xác nhận ưu tiên build draft.** `/br` (nhánh nặng) đã có id ổn định từ lâu; nhánh `/propose`+`/plan` (dùng hàng ngày hơn `/br`) vẫn thiếu cổng — draft đã thiết kế xong, phần còn lại chỉ là build 4 task đã liệt kê trong SPEC |

## Nên claim thêm (ưu tiên theo effort, thấp→cao)

1. **Build draft `140726-spec-kit-traceability`** (rẻ nhất — SPEC đã viết, còn 4 task: id+tag ở `/propose`, SC đo-được-không-dính-công-nghệ, PLAN khai `**Thoả:**`, cổng truy vết ở `proposal_complete.py`). Trend cộng đồng 19/08 xác nhận đây không phải đầu tư sai hướng.
2. **5 trường thiết kế MSCE làm input cho GH#10** — mỗi skill tự-sinh mang `evidence_link` + `applicability_boundary` + `decision_guidance` + `verification_rule` + `reliability_estimate`, thay vì rule/skill stub trơn như `failure-flywheel` hiện tại.
3. **Trust/conflict scoring cho R19** (effort vừa — thêm một bước: khi 2 điểm cuối evidence trong cùng câu trả lời mâu thuẫn nhau, hiện luật không có cơ chế phát hiện; học "provenance relations" + "trust functions" của academic framework).
4. **Compile-tối-ưu-prompt kiểu DSPy cho `agent()` trong Workflow tool** (effort cao nhất — đổi mô hình thực thi từ prompt tĩnh viết tay sang module khai báo tự tối ưu; cần thử nghiệm riêng trước khi cam kết).

Không raise GH issue tự động trong phiên này — theo `/raise-issue`, đây là hành động công khai cần user xác nhận trước. Mục 2 và 3 sẵn sàng làm input nếu user muốn mở comment vào GH#10 hiện có; mục 1 đã có issue-tương-đương ở dạng draft, không cần issue mới.

## Nguồn
- Web: "The best AI agent frameworks in 2026" (LangChain) — https://www.langchain.com/resources/ai-agent-frameworks
- Web: "Top Agentic Frameworks for Building Applications 2026" (JetBrains Blog) — https://blog.jetbrains.com/pycharm/2026/06/top-agentic-frameworks-for-building-applications-2026/
- Web: "The Code Agent Orchestra — what makes multi-agent coding work" (Addy Osmani) — https://addyosmani.com/blog/code-agent-orchestra/
- arXiv 2606.04990: "From Agent Traces to Trust: A Survey of Evidence Tracing and Execution Provenance in LLM Agents" (2026) — https://arxiv.org/html/2606.04990
- arXiv 2607.16621: "From Memory to Skills: Evidence-Grounded Co-Evolution Governance for Long-Horizon LLM Agents" (2026) — https://arxiv.org/html/2607.16621
- GitHub PR: olibartfast/model-stager#4 "Add spec-driven constitution..." — https://github.com/olibartfast/model-stager/pull/4 (2026-08-15, retrofit specs theo khoá DeepLearning.AI Spec-Driven Development)
- Reddit r/LocalLLaMA: "What sandbox are you all using for AI agents?" — https://www.reddit.com/r/LocalLLaMA/comments/1vrps78/ (2026-08-18, 42pts/111cmt, tín hiệu lặp lại chủ đề sandbox đã quét 18/08 — không tính là trục mới)
- Reddit r/AI_Agents: "Are AI agents creating more complexity than they remove?" — https://www.reddit.com/r/AI_Agents/comments/1vrs892/ (2026-08-18, 5pts/18cmt, tín hiệu mỏng, ghi nhận không đưa thành trục)
- Đối chiếu nội bộ: `fdk/CAPABILITIES.md`, `skills/graph-mode/SKILL.md`, `llmwiki/wiki/sources/draft/140726-spec-kit-traceability.md`, `llmwiki/wiki/sources/ISSUES.md` (GH#10, GH#12 xác nhận vẫn `open` qua grep trực tiếp)

## Origin
Kích hoạt bởi yêu cầu trực tiếp của user (phiên 2026-08-19): "cài last30days repo nếu chưa có" + "đánh giá những gì repo của chúng ta làm được so với các ý tưởng mới trên thế giới ... ra 1 file /llmwiki/innovation/ddmmyy-innovation.md, so sánh phải có dạng cột trực quan cái cũ mới cái mới". Kiểm tra cài đặt: `~/.agents/skills/last30days` (đích symlink của `~/.claude/skills/last30days`) chỉ có `SKILL.md`, thiếu `scripts/last30days.py` — bản cài BỊ HỎNG, không phải "chưa cài". Repo nguồn đầy đủ (git remote `github.com/mvanhorn/last30days-skill`, branch `main` up-to-date) đã tồn tại sẵn ở `~/orca/last30day/last30days-skill`; đã `rm` thư mục cài hỏng rồi tạo lại symlink trỏ vào `skills/last30days` trong repo đó — sửa xong, không phải cài mới từ đầu. Dùng `python3.14` (đủ chuẩn 3.12+ của skill). Chạy engine với `--plan` tay (3 subquery tự soạn) vì cả 3 chủ đề đều là khái niệm chung, không phải named-entity — bỏ qua bước resolve X-handle/GitHub-user theo đúng luật skip của chính SKILL.md. Engine tự động coi chuỗi có dấu `:` trong topic là 2 "thực thể" nên chạy ở vs-mode với evidence khá mỏng (entity-miss demotion) — bù lại bằng 2 lượt `WebSearch` tổng quan trước đó, cho phần lớn chất liệu thật của bài này.
